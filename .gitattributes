# Treat font binaries as binary — never apply line-ending (CRLF/LF) conversion,
# which would corrupt them. (core.autocrlf=true on this machine.)
*.ttf binary
*.woff binary
*.woff2 binary
*.otf binary
