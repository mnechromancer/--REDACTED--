# Roadmap — v3

Authority: `reset_v3_intake.md` (the plan) + `amber_build_decisions.md` (the decisions).
Prior roadmaps (the v1 epic/milestone table, the v2 phase ledger) live in git history and
`archive/`; the v2 phase record is preserved in the decisions log.

## Phase ledger

- **Phase 0 — decisions + docs consolidation.** §7 A–D answered; the doc set collapsed to
  one live spec per concern + `archive/`; `CLAUDE.md` rewritten. ✅ *done 2026-07-04.*
- **Phase 1 — the frame's engine.** `collection: local | inbound` + `day`; day-gated
  reachability (the tray is open — seed-plus-closure retired); the 4 PM transmittal
  turnover (`end`); `note`; the mail store + `mail`; first contact retargeted to the
  first honest commit (v3-C, pulled forward). ✅ *done 2026-07-04 — 164 tests, corpus/
  check/build green; details in the decisions log.*
- **Phase 2 — the opening.** Boot/login; day-1 mail (the supervisor's brief states the
  premise); the real shelf (~5–6 files) + day-1 batch (4 files ramped by word kind);
  Quippy's rebuilt entrance (rides the batch; manifests after the player's first
  forged-and-committed citation; routes back to a blank left behind). **Retires the v2
  corpus** (001–009 mined for prose, self-file re-authored). The next thing the human
  plays.
- **Phase 3 — the OS.** `ls` / `man` / `status` / `log`; the concordance (`xref`/`grep`
  over reachable unredacted text, forgeable hit spans); `diff`; `verify` as transmittal QC.
- **Phase 4 — content: the context puzzles.** Day 2–3 batches; the directory-discrepancy
  puzzle; winnable inference slots; patterns 2–6 (`reset_v3_intake.md` §6); the new
  registry seeds.
- **Phase 5 — Quippy's widening.** Ghost-text, summarize, batch fill, memory-across-the-
  wipe; band tuning over the day structure.
- **Phase 6 — the graph.** `map` — the wiki-graph as a first-class AMBER tool.
- **Phase 7 — reference corruption.** Quippy fills rewrite the cited references so the
  corpus closes over the lie (`design_note_quippy_corruption.md`) — the largest engine
  change; last.

## Status

| Phase | State |
|---|---|
| 0 — Decisions + consolidation | ✅ done (2026-07-04) |
| 1 — Frame engine | ✅ done (2026-07-04) |
| 2 — Opening | next |
| 3 — OS | after 2 |
| 4–7 | horizon |

Each phase ends playable. Update this table at each phase close; record decisions in
`amber_build_decisions.md` as they land.
