# Planning

The execution layer: what we're doing now and next. Reference specs (what the game *is*)
live one level up in `vault/docs/`; method (how the team works) is `agents.md`; history is
`../archive/`.

**Current direction: the v3 frame** (2026-07-04) — the receiving site, the daily batch,
the 4 PM erasure. Read in this order:

1. **`reset_v3_intake.md`** — the pivot: frame, mechanics, first-batch design, puzzle
   patterns, consolidation. §7 decisions answered (see the log).
2. **`amber_build_decisions.md`** — the append-only decisions log, v1→v3. The ground truth
   for "what was decided and why."
3. **`roadmap.md`** — the v3 phase ledger + status.
4. **`sprint_process.md`** — the repeatable runbook (evergreen method).
5. **`design_note_quippy_corruption.md`** — Phase 7 spec (spec'd, build deferred).

Rules of the folder: decisions append to the log at the moment they're made; the roadmap's
status table updates at each phase close; completed handoffs and superseded plans move to
`../archive/` rather than being rewritten — history stands.
