# Game entries live here.
# build-corpus.ts parses every *.md in this folder (files prefixed _ are ignored).
# Authoring contract, worked example, and the winnability spine: vault/docs/entry_authoring.md.
# Designations: REF-## = the Site-81C shelf (local, unredacted); SCP-41B-### = inbound
# Site-41B documents in hundred-blocks by day (day 1 = 101–104); SCP-41B-000 = the self-file.
# Per-file loop: author → npm run build:corpus → npm run test. Never batch.
